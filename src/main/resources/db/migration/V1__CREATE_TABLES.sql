CREATE TABLE users
(
   id bigint --part of primary key,
   email varchar(50),
   first_name varchar(5O),
   last_name varchar(100),
   password varchar(255),
   role varchar(20),
   status varchar(20)
);
